name: CI Project3

on:
  push:
    branches:
      - main

jobs:
  hello-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test simple
        run: echo "CI fonctionne pour Project3"
      - name: Vérification syntaxe PHP
        run: |
          find . -name "*.php" -print0 | xargs -0 -n1 php -l

